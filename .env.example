# Copy to .env for local development. Render reads these from its Environment tab, not from this file.
# Optional: where gathering invitation requests are posted as JSON. Leave unset to fall back to email.
VITE_EVENTS_WEBHOOK_URL=
